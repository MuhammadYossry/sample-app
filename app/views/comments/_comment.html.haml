= div_for comment do
  %p
    %b= h(comment.title)
    %i
      Posted #{time_ago_in_words(comment.created_at)} 
      by #{link_to comment.user, display_all_path(comment.user)}
    %br
    = h(comment.body)